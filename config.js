const config = {
    filter: {
      includePastEvents: true,
      includeFutureEvents: true,
    },
    format: {
      timezone: 'UTC',
      dateFormat: 'YYYY-MM-DD',
    },
  };
  
  export default config;